# === BAZA DANYCH H2 (w pami?ci) ===
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# === ZMIENNE ADMINISTRATORA (Wymagane przez AdminInitializer) ===
# Klasa AdminInitializer u?ywa @Value("${APP_ADMIN_EMAIL}"), wi?c klucz musi by? identyczny
APP_ADMIN_EMAIL=admin@test.pl
APP_ADMIN_PASSWORD=admin123

# === ZMIENNE MAILOWE (Wymagane przez main/resources/application.properties) ===
# Musimy je zdefiniowa?, bo Spring próbuje rozwi?za? ${SPRING_MAIL_...}
SPRING_MAIL_USERNAME=test@fake.com
SPRING_MAIL_PASSWORD=fake123

# === KONFIGURACJA MAILA DLA TESTÓW ===
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test@fake.com
spring.mail.password=fake123
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.ssl.enable=false

# === ZMIENNE BAZOWE (Dla pewno?ci, je?li co? jeszcze z nich korzysta) ===
SPRING_DATASOURCE_URL=jdbc:h2:mem:testdb
SPRING_DATASOURCE_USERNAME=sa
SPRING_DATASOURCE_PASSWORD=password